import {isSomeBombOnLocation} from "./Missile";

describe('isSomeBombOnLocation', () => {
    it('should return true if there is a bomb in a given state on given location', () => {
        isSomeBombOnLocation()
    });
});